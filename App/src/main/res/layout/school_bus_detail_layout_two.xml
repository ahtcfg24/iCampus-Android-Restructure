<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <RelativeLayout
        android:id="@+id/relativeLayout1"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_marginTop="2dp"
        android:layout_marginLeft="2dp"
        android:layout_marginRight="2dp"
        android:background="@drawable/rect"
        android:gravity="center" >

        <ImageView
            android:id="@+id/busLineBodyImageView"
            android:layout_width="wrap_content"
            android:layout_height="80dp"
            android:layout_alignParentTop="true"
            android:layout_centerHorizontal="true"
            android:src="@drawable/bus_detail_arrow_right" />

        <TextView
            android:id="@+id/titleTextViewHead"
            android:layout_width="wrap_content"
            android:layout_height="80dp"
            android:layout_alignBottom="@+id/busLineBodyImageView"
            android:layout_alignParentTop="true"
            android:layout_toLeftOf="@+id/busLineBodyImageView"
            android:gravity="center"
            android:text="TextView"
            android:textSize="20sp" />

        <TextView
            android:id="@+id/titleTextViewBody"
            android:layout_width="wrap_content"
            android:layout_height="80dp"
            android:layout_alignBottom="@+id/busLineBodyImageView"
            android:layout_alignParentTop="true"
            android:layout_toRightOf="@+id/busLineBodyImageView"
            android:gravity="center"
            android:text="TextView"
            android:textSize="20sp" />
    </RelativeLayout>

    <ListView
        android:id="@+id/busDetailListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:paddingLeft="20dp"
        android:layout_below="@+id/relativeLayout1"
        android:divider="@null" >
    </ListView>

</RelativeLayout>